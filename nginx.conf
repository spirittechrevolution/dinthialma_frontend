server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Compression gzip pour les assets statiques
    gzip on;
    gzip_types text/plain text/css application/json application/javascript
               text/xml application/xml application/xml+rss text/javascript
               image/svg+xml;
    gzip_min_length 1024;

    # Cache long terme pour les assets Vite (contiennent un hash dans leur nom)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # SPA fallback : toutes les routes inconnues → index.html
    # Nécessaire pour React Router (BrowserRouter)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Health check pour Docker/Traefik
    location = /health {
        return 200 "ok";
        add_header Content-Type text/plain;
        access_log off;
    }

    # Désactiver les logs d'accès pour les assets courants
    location /favicon.ico {
        access_log off;
        log_not_found off;
    }
}
